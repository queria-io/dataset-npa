[project]
name = "dataset-npa"
version = "0.1.0"
description = "NPA traffic accident dataset for Queria"
requires-python = ">=3.13"
dependencies = [
    "queria>=0.13.0",
    "dbt-core>=1.10.0",
    "dbt-duckdb>=1.10.0",
]
